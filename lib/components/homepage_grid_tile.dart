import 'package:flutter/material.dart';

class HomePageGridTile extends StatelessWidget {
  const HomePageGridTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
